import styled from 'styled-components';

const AddWorkoutContainer = ({ className }: { className?: string }) => {
	return <div className={className}>333</div>;
};

export const AddWorkout = styled(AddWorkoutContainer)`
	margin: auto;

`;
